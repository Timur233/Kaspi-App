import '../scss/main.scss';

// const categoryValidator = (node) => {

// }

// class CategoryValidator {
//     constructor(node, type) {
//         this.node = node,
//         this.type = type;
//     }
// }

// const validator = new CategoryValidator(document.createElement('div'), 'default');
